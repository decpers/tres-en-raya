export const d = document;
export const body = d.querySelector('body');
export const message = d.getElementById('message');

const first = d.getElementById('info__player1');
const second = d.getElementById('info__player2');
export const containers = {
    first: first,
    second: second
}